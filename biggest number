// Online C compiler to run C program online
#include <stdio.h>

int main() {
    int a,b,c;  
    printf("enter the numbers ");
    scanf("%d%d%d",&a,&b,&c);
    if(a>b||a>c){
        printf("the biggest number out of 3 numbers is %d",a);
    }
    else if(b>a||b>c){
        printf("the biggest number out of 3 numbers is %d",b);
    }
    else if(c>a||c>b){
        printf("the biggest number out of 3 numbers is %d",c );
    }
}
